// Entity types exposed by this plugin
export const ENTITY_TYPES = [] as const;

// Data keys
export const DATA_KEYS = {} as const;

// Action keys
export const ACTION_KEYS = {} as const;

// Job keys
export const JOB_KEYS = {} as const;

// Tool keys
export const TOOL_KEYS = {} as const;

// UI slots
export const UI_SLOTS = {} as const;
